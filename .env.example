# Copy to .env locally. Never commit .env or real credentials.
DATABASE_URL=postgres://rigrx:rigrx@localhost:5432/rigrx
BASE_URL=http://localhost:3000
PORT=3000
# The number used for the initial administrator account:
ADMIN_PHONE=
# Development only: missing keys currently enable simulated SMS/payments.
# Configure all values before using real customer accounts or billing.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
